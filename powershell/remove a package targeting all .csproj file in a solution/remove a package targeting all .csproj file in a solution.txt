Get-ChildItem -Path . -Recurse -Include *.csproj | ForEach-Object {
    Write-Host "Attempting to remove OpenAI from $($_.FullName)"
    dotnet remove ($_.FullName) package OpenAI
    Write-Host "--------------------------------------------------"
}